As an admin user, I can browse all the users (list of users).
As an admin user, I can filter all the users via different criterias.
As an admin user, I can change the sort order of the displayed users.
